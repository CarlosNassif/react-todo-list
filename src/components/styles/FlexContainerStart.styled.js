import styled from 'styled-components';

import { FlexContainer } from './FlexContainer.styled';

export const FlexContainerStart = styled(FlexContainer)`
  justify-content: flex-start;
  align-items: flex-start;
  padding: 8px;
  text-align: center;
  max-width: 90%;
  width: 600px;
`;
